var config = {
    db: { 
    'database':'test_sqz', 
    'user': 'root', 
    'password': 'root', 
    'dialect': 'mysql',
    },
    host: 'localhost',
    restPort: 3000,
    socketPort: 8000
}

module.exports = config;
